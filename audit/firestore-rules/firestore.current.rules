rules_version = '2';

/* The rule that is LIVE on the project today, reproduced verbatim so the
   test suite can demonstrate what it permits. Do not publish this file. */

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
