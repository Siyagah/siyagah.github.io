{
  "name": "siyagah-firestore-rules-audit",
  "private": true,
  "type": "module",
  "description": "Emulator-backed proof for the proposed Siyagah Firestore rules. Not part of the app.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project siyagah-rules-audit \"node tests/rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.0.0"
  }
}
